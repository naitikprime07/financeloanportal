# Cloudflare Pages Redirects Configuration
# This replaces the redirects from vercel.json

# Redirect non-www to www (if that's your preference)
# Adjust this based on your actual domain setup
https://financeloanplatform.com/* https://www.financeloanplatform.com/:splat 301!

# SPA fallback - serve index.html for all routes that don't match files
# This ensures client-side routing works correctly
/*  /index.html  200
